-- Migration template for SeeMyCV
-- To create a new migration:
-- 1. Copy this file and rename it with a timestamp: YYYYMMDD_HHmmss_description.sql
-- 2. Write your SQL statements
-- 3. Run: psql -U your_user -d your_database -f scripts/migrations/YYYYMMDD_HHmmss_description.sql

-- Example migration structure:
-- ============================================
-- UP (Apply migration)
-- ============================================

-- CREATE TABLE example_table (
--   id SERIAL PRIMARY KEY,
--   name VARCHAR(255) NOT NULL,
--   created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- ============================================
-- DOWN (Rollback migration - for reference)
-- ============================================

-- DROP TABLE IF EXISTS example_table;
